<?xml version="1.0" encoding="utf-8" ?>
<ContentView xmlns="http://schemas.microsoft.com/dotnet/2021/maui"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             xmlns:viewmodels="clr-namespace:KesifUygulamasiTemplate.ViewModels"
             x:Class="KesifUygulamasiTemplate.Views.OllamaPanel">

    <ContentView.BindingContext>
        <viewmodels:OllamaPanelViewModel />
    </ContentView.BindingContext>

    <VerticalStackLayout Padding="10">

        <!-- Aç/Kapat Butonu -->
        <Button Text="💬 Ollama Panel Aç/Kapat" 
                Command="{Binding PanelToggleCommand}" />

        <!-- Panel İçeriği -->
        <StackLayout IsVisible="{Binding PanelAcik}" Padding="10" BackgroundColor="#f0f0f0">

            <Entry Placeholder="Sorunuzu yazın..." 
                   Text="{Binding Soru}" />

            <Button Text="Sor" 
                    Command="{Binding SoruSorCommand}" Margin="0,10,0,0"/>

            <ScrollView HeightRequest="200">
                <Label Text="{Binding Cevap}" 
                       LineBreakMode="WordWrap"
                       TextColor="Black"/>
            </ScrollView>

        </StackLayout>
    </VerticalStackLayout>
</ContentView>
